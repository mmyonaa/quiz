# Changelog

daily.quiz의 버전별 변경 기록. [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) 형식과
[유의적 버전(SemVer)](https://semver.org/lang/ko/)을 따른다.

- 버전의 기준은 `package.json`의 `version`이며, 릴리스마다 git 태그(`v1.0.0` 형식)를 붙인다.
- 일감 단위 기록은 [GitHub Project](https://github.com/users/mmyonaa/projects/6)와 이슈에 남긴다.

## [2.1.0] - 2026-08-28

개념 노트를 **읽는 도구**로 다듬은 릴리스. 문항 91주제·해설 515개가 한 축으로만 열려 있어
"지금 어디를 읽고 있고 다음은 어디인지"가 화면에 없었다. 좌측 목차가 그 축을 맡는다.
콘텐츠는 그대로이고 전부 UI 레이어의 변화다.

### 추가

- **개념 노트 좌측 목차에 세부 주제** — 영역·과목만 있던 목차 밑에 그 안의 주제를 두 번째 단으로 편다.
  읽는 위치를 따라 현재 주제가 강조되고(스크롤 스파이) 목차가 부드럽게 따라 움직인다.
  난이도 필터로 사라진 주제는 목차에서도 빠져 죽은 링크가 남지 않는다.
- **주제 목록 아코디언** — 영역·과목마다 펼침 화살표를 둔다. 링크가 아니라 버튼이라 눌러도 이동하지 않고,
  모바일 드로어도 닫히지 않는다. 목차는 모든 영역의 주제를 담고, 다른 영역의 주제는 그 영역 페이지의
  앵커로 바로 건너뛴다. 한 번에 하나만 펼친다 — 여섯 영역이 다 열리면 목차가 화면을 넘긴다.
- **영역 줄 위아래 고정** — 주제가 26개까지 늘어나도 위로는 지나온 영역이, 아래로는 남은 영역이
  항상 한 번에 닿는 자리에 남고 그 사이에서 주제만 스크롤된다.
- **좁은 화면 2단 드로어** — 헤더의 '목차' 버튼으로 좌측 목차를 연다. 1단은 영역 목록, 화살표를 누르면
  그 영역의 주제 목록이 오른쪽에서 덮으며 들어온다(← 뒤로). 한 화면에 항상 짧은 목록 하나만 남는다.
  표시만 화면 폭에 따라 갈리고 상태는 하나다 — 열린 판은 언제나 하나뿐이다.
- **목차 검색** — 91주제가 여섯 영역에 나뉘어 있어, 어느 영역에 있는지 모르는 주제는 목록을 훑어서는
  못 찾았다. 목차 맨 위 입력 한 줄로 영역 경계를 넘어 거른다. 결과는 새로 그리지 않고 있는 목록을
  거르므로 영역 줄이 그대로 결과의 머리글이 되고, 다른 영역의 주제는 그 영역 페이지의 앵커로 이어진다.
- **헤더 상단 고정** — 스크롤을 내려도 메뉴·테마·목차 버튼에 닿는다. 높이는 로그인 여부·줄바꿈으로
  달라지므로 실측해 `--top-h`로 공유하며, sticky 주제 제목·풀이 상단바·답안지·앵커 오프셋이 이를 참조한다.

### 변경

- **실기 대비 용어 병기 12주제 보강** — 실기는 답을 직접 쓰므로 한글 읽기와 영문·약어 풀네임이 둘 다 필요하다.
  파일럿에서 "옵저버 패턴"이 오답 처리된 것도 개념 노트가 패턴 이름을 영문으로만 적어 둔 탓이었다.
  - 한글 읽기가 없던 곳 — 디자인 패턴 23종에 `싱글톤(Singleton)`·`옵서버(Observer)` 식으로 한글을 붙였다.
  - 약어 풀네임이 없던 곳 — `FIFO(First In First Out)`, `LRU(Least Recently Used)`,
    `SRP(Single Responsibility Principle)`, `BFS(Breadth First Search)`, `CIDR(Classless Inter-Domain Routing)` 등.
  - 영문이 아예 없던 2주제 — 블랙박스/화이트박스 테스트(동등 분할·경계값 분석·원인-효과 그래프·오류 예측과
    구문→분기→조건→경로 커버리지), 보안 위협 분류(자산·위협·취약점·위험의 층위를 새로 정리).
  - 그 밖에 키의 종류(슈퍼키·후보키·기본키·대체키·외래키와 개체·참조 무결성), 고급 SQL, 대칭키/비대칭키,
    Big-O(시간복잡도·공간복잡도), IP 서브넷팅, HTTP.
- **팔레트를 daily.mcp와 정렬** — 쿨 뉴트럴 베이스 + 코랄 액센트로 맞추고, 텍스트 대비에 못 미치던
  코랄 원색을 면·테두리용(`--accent`)과 텍스트·버튼용(`--accent-strong`)으로 갈랐다.
  좌측 색 보더 패턴 다섯 곳을 걷고, 초록은 정답·합격 의미로만 남겼다.
- **실기 확인 문제 카드 정돈** — 카드 맨 위에 홀로 떠 있던 점선을 없애고 정답란 구분선·라벨 폭을 손봤다.
- **확인 문제 채점 결과에 연한 면** — 글자색만으로는 긴 해설 사이에서 눈에 띄지 않아, 정답과 내가 고른
  오답 뒤에 배경을 깔았다.
- **좁은 화면 여백·버튼 균형** — 페이지와 패널이 각각 물고 있던 가장자리 여백을 한쪽 40px → 30px로 줄였다.
  개념 노트 소프트웨어공학 기준 문서 높이가 5.3% 짧아진다. 주변 칩보다 혼자 크던 시작 버튼도 낮췄다.
- **목차 스크롤바** — 200px 남짓한 목차에서 기본 스크롤바는 폭도 색도 과했다. 얇은 막대로 낮추고
  스크롤바 자리를 늘 비워 둔다(주제를 펼칠 때 목록이 옆으로 밀리지 않게).

### 수정

- **모바일 드로어의 가로 스크롤** — 세로만 `auto`로 두면 가로도 `auto`가 된다. 닫힌 2단 판이 화면 밖
  오른쪽에 서 있어 그만큼 가로로 스크롤됐다(303 → 606px).
- **닫힌 드로어의 그림자 누출** — 드로어는 화면 밖에 있어도 그림자는 화면 안으로 번진다. 열릴 때 튀지
  않게 그림자를 미리 달아 둔 것이 원인이었고, 모양은 두고 색만 투명↔불투명으로 전환하게 고쳤다.
- **닫힌 판 안의 링크가 탭 순서에 남던 문제** — 보이지 않는 링크로 포커스가 가면 잘라 둔 영역이 밀렸다.
- **스크롤 스파이가 현재 주제를 고정된 줄 뒤에 두던 문제** — 목차의 시야는 전체가 아니라 위아래로
  고정된 영역 줄 사이다.

### 참고

- 앞서 "병기 0개 25주제"라고 보고한 수치는 정규식이 좁아 나온 값이었다. `전위(preorder, 루트→왼쪽→오른쪽)`처럼
  괄호 안에 쉼표나 한글이 섞이면 잡지 못했다. 다시 재면 12주제였고, 영문이 아예 없는 주제는 2개였다.
  보강 후 91주제 전부가 영문 표기를 갖는다(주제당 평균 4.6개).

## [2.0.0] - 2026-08-28

필기만 다루던 사이트에 **실기(필답형)** 를 얹은 릴리스. 시험 종류가 하나 늘어나 메이저를 올린다.
실기는 보기가 없어 답을 직접 쓰므로, 4지선다를 전제한 기존 구조로는 담기지 않았다.

### 추가 — 실기 파일럿

- **실기 콘텐츠 컬렉션** (`src/data/practical.json`, 20문항) — 필기와 별도 스키마다. 보기·정답 인덱스 대신
  답란별 허용 표기 목록(`answers: string[][]`)을 갖고, 유형은 단답형·계산형·코드형·SQL형·약술형 5종이다.
  필기와 **같은 91주제를 공유**하므로 `area`·`subject`는 주제에서 풀어 얹고 문항이 중복해 적지 않는다.
- **채점 엔진** (`src/lib/practical-grade.ts`) — 필답형의 본질적 난제인 "같은 답의 다른 표기"를 두 겹으로 흡수한다.
  정규화(NFKC·소문자·공백 제거·장식 문자 제거·낱말 하이픈)로 표기 요동을 뭉개고, 뜻이 다른 낱말은 문항이 허용 표기로 나열한다.
- **`/practical` 라우트** — 유형·난이도·문항 수를 골라 연습하거나, 실기 모의고사(20문항·150분·문항당 5점·60점 합격)를 본다.
  답안 작성란 사이드바는 필기 OMR 컴포넌트를 재활용해 쓴 답을 그대로 훑어볼 수 있게 했다.
- **약술형 자가 채점** — 자동 채점하지 않는다. 내 답안과 모범답안을 나란히 놓고 핵심어 포함 수를 세어 준 뒤 O/X는 사용자가 찍는다.
- **"이 표기도 정답 처리"** — 자동 채점이 놓친 표기를 사용자가 인정시키면 localStorage에 쌓여 다음부터 정답 처리된다.

### 추가 — 개념 노트 통합

- **개념 노트는 필기·실기로 나누지 않는다.** 실기 20문항을 만들면서 새로 필요한 주제가 0개였다 —
  개념 집합이 같고, 다른 것은 요구하는 산출 형태(고르기 vs 쓰기)뿐이다. 노트를 쪼개면 같은 도입부가
  두 벌 생기고 주제의 단일 정의가 깨진다. 대신 주제 **안에서** 문항을 구분한다.
- **주제별 실기 확인 문제** — 필기 확인 문제(알아보기) 아래에 실기 확인 문제(떠올리기)를 잇는다.
  보기가 없으므로 답을 떠올린 뒤 '정답 보기'로 펼쳐 대조하며, 허용 표기와 해설을 함께 보여준다.
- **실기 출제 형태 뱃지** — `실기 코드형`처럼 이 주제가 실기에서 어떻게 나오는지 개념 옆에 표시한다.
  `practical.json`에서 주제별 유형을 모아 만들므로 추가 데이터가 없다. 노트 목차에도 영역·과목별 실기 개수를 얹었다.

### 변경

- 난이도 필터 칩 카운트를 화면에 실제로 놓인 카드 수(필기 해설 + 실기 확인 문제)와 맞췄다.
  전에는 필기만 세어 "전체 91"인데 카드는 96개인 불일치가 있었다.
- 답안지 2단 배치의 CSS 훅을 `#exam.mock` → `#exam.with-sheet`로 바꿨다. 모의고사 전용이 아니라
  "답안지가 붙는 화면"의 속성이며, 실기 화면은 연습 모드에서도 답안지를 늘 띄운다.
- 헤더 메뉴를 "문제 풀기" → "필기 문제" + "실기 필답"으로 나눴다.

### 참고

- 파일럿의 목적은 문항 확충이 아니라 **채점 정규화 검증**이었다. 20문항에 표기를 흔들어 넣어(대소문자, 띄어쓰기,
  하이픈, 한글/영문, 단위 접미사) 돌린 결과 자동 채점 18문항 중 17문항이 통과했고, 놓친 하나는
  "옵저버 패턴"(허용 목록에 "옵서버 패턴"만 있었다)이었다. 허용 표기를 보강하고 escape hatch가 동작함을 확인했다.
- 다음 단계: 문항 100+ 확충 → 도입부의 영문 병기 보강(91주제 중 25개가 `한글(English)` 병기 0개이며,
  그 안에 실기 출제 주제인 `design-patterns`·`ip-subnetting`이 들어 있다) → Supabase 저장·오답 기록 연동
  (`saved_questions`·`wrong_answers`가 필기 id를 전제하므로 조회 경로를 두 은행 합집합으로 넓혀야 한다).

## [1.6.0] - 2026-08-27

2021년 기출 2회분(5월·8월)을 대조해 출제기준이 명시하지 않는 "목록형 디테일"을 메운 릴리스.

### 추가

- **신규 2주제 70문항** (은행 445 → 515, 주제 89 → 91) — 코드 설계와 코드 오류, 오류 제어와 흐름 제어. 이로써 **91개 주제가 모두 1문항 이상**을 갖게 됐다.
  - `code-design` — 코드 6종(순차·블록·십진·표의 숫자·연상·합성)과 코드 오류 6종(Addition·Omission·Transcription·Transposition·Double Transposition·Random). 오류 유형은 자릿수 예시를 주고 유형을 고르는 형태로 출제된다.
  - `error-flow-control` — 후진(BEC)/전진(FEC) 오류 수정, ARQ 4종(정지-대기·Go-Back-N·선택적 재전송·적응적), 패리티·CRC·해밍 코드, 슬라이딩 윈도우.

### 변경

- **기존 38주제 도입부 보강** — UML 클래스 관계 6종, 시스템 테스트 종류와 확인/검증·결함 용어 계층, 방화벽 구축 유형 4종과 오용/이상 탐지, 관계대수 나누기·카티션 프로덕트, 균형 이진 탐색 트리(AVL·레드-블랙·2-3), CSMA/CD와 CSMA/CA, well-known 포트, 버전 관리 명령 용어와 저장 방식 3분류, 애자일 방법론 종류와 XP 5가치, 낙관적 검증·MVCC와 로킹 단위, 클라크-윌슨·만리장성 모델, 솔트와 레인보우 테이블, IPSec 전송/터널 모드, ISO/IEC 12207 3분류, 요구사항 검증 항목, 힙 정렬, 페이지 크기 트레이드오프, FCB와 파일 디스크립터, C 표준 헤더와 ASCII, 파이썬 range·self, 자바스크립트 프로토타입 체인 등.

### 참고

- 두 회차 분석 결과 **구조적 갭은 사실상 소진**됐다. 출제기준 전수 대조(v1.5.0) 이후 남은 것은 기준서가 항목 이름만 적고 넘어가지만 시험은 반복해 묻는 세부 목록이었고, 이번 릴리스가 그 층을 메웠다.
- 부정형 발문 비율 43%, 정답 인덱스 분포 0:127 / 1:138 / 2:131 / 3:119로 균형 유지.

## [1.5.0] - 2026-08-27

큐넷 공식 출제기준과 전수 대조해 커버리지 사각지대를 메운 릴리스.

### 추가

- **신규 8주제 50문항** (은행 395 → 445, 주제 81 → 89) — 인터페이스 설계, 인터페이스 구현과 검증, 고급 SQL(서브쿼리·윈도우 함수·ROLLUP/CUBE), 물리 데이터베이스 설계, 데이터 전환과 정제, SW 개발 보안(Secure SDLC와 취약점 7분류), 개발환경 구축, 현행 시스템 분석.

### 변경

- **대조 방식을 표본에서 전수로** — 그동안은 기출 시험지 2회분을 표본 삼아 갭을 찾았으나, 이번에는 큐넷 공식 출제기준(HWP 원본)의 세부항목 47개를 89개 주제 도입부와 하나씩 대조했다. 결과는 완전 미커버 9건, 얕은 커버리지 18건. 특히 **2과목 「인터페이스 구현」과 3과목 「데이터 전환」이 주요항목 단위로 통째 비어 있었다** — 과목당 20문항 기준으로 각각 3~4문항씩 나오는 자리다.
- **기존 22주제 도입부 보강** — CASE 도구, 감성공학과 ISO 9241, 연계 방식과 데이터 명세화, 소스코드 품질분석 도구와 순환복잡도, 테스트 원리 4가지와 결함관리 프로세스, 프로그램 디버깅, 협업도구, IPC, APM, 옵티마이저와 실행계획, DB 이중화, DB 암호화 4방식, API와 REST·SOAP, 서버 개발 프레임워크, 라이브러리와 데이터 입출력, SPICE·ISO 12207·전자정부 표준프레임워크, CBD 방법론, 보안 아키텍처와 ISO 27001·Secure OS, DW·데이터마이닝·OLAP, 환경변수, 서브쿼리, 논리 모델 품질검증.

### 참고

- 대조 기준은 2023~2025년판 출제기준이다. 2026년판은 큐넷이 HWP 첨부에서 리포트 뷰어로 서비스 방식을 바꿔 본문을 추출하지 못했다. 유효기간이 1년뿐이라 실질 개정보다는 연장·경미 수정으로 보이나 확인하지는 못했다.
- 출제기준에 없는데 다루고 있는 주제로 `http-status-codes`(5문항)가 확인됐다. 실무에는 유용하나 시험 대비로는 비중이 과하다.

## [1.4.0] - 2026-08-27

실제 시험 과목을 계층으로 얹고, 2022년 3월 기출을 대조해 개념을 보강한 릴리스.

### 추가

- **시험 과목 계층** — 정처기 필기의 5과목(소프트웨어 설계·소프트웨어 개발·데이터베이스 구축·프로그래밍 언어 활용·정보시스템 구축 관리)을 영역 위 계층으로 얹었다. 과목과 영역은 다대다여서(소프트웨어공학은 1·2·5과목에, 프로그래밍은 2·4과목에 걸치고, 운영체제·네트워크는 둘 다 4과목에 들어간다) 과목을 영역이 아니라 주제(topic)에 붙였다. 81개 주제를 전수 매핑하고 스키마에서 검증한다.
- **과목별 개념 노트** `/notes/s/<slug>` 5개 신설. 목차에는 '시험 과목' 카드가 영역 위에 놓이고, 과목마다 주제 수와 걸쳐 있는 영역을 함께 보여준다.
- **문제 풀이에 과목 선택** — 과목과 영역을 하나의 선택 그룹으로 통합해 어느 쪽으로든 범위를 좁힐 수 있다.
- **신규 7주제 45문항** (은행 350 → 395, 주제 74 → 81) — 상호배제와 프로세스 동기화(74주제 중 유일하게 비어 있던 영역), 리눅스 시스템 관리, 스토리지와 고가용성, 소프트웨어 재공학과 재사용, 구조적 분석·설계 표기법, E-R 모델과 표기법, 무선·근거리 통신 보안 위협.

### 변경

- **모의고사가 실제 시험 구조와 일치** — 임의의 영역 배분(15/15/20/20/15/15)에서 5과목 × 20문항으로 바꿨고, 과락 판정도 영역이 아니라 과목 기준으로 교정했다.
- **기존 30주제 도입부 보강** — ISO 25000(SQuaRE)·FTR 지침, WBS, 객체지향 분석 방법론, 언어 선정 기준, 파일 조직법, 4NF·5NF, 관계해석 정량자, CREATE TABLE 제약조건·DROP CASCADE, UNION/UNION ALL, 병행제어·2PL, 스크럼 마스터·테일러링, 계층별 프로토콜, ICMP·ARP, IPv6 심화, FAT vs NTFS, 그리고 C 비트·시프트 연산자와 우선순위, C 2차원 배열·배열 포인터, Python 반복문·딕셔너리 접근, Java 배열 참조 전달.
- **GitHub Actions 액션 업그레이드** — checkout v7, setup-node v7, upload-pages-artifact v5, deploy-pages v5로 올려 Node 20 deprecated 경고를 없앴다.

### 내부

- 영역 페이지와 과목 페이지가 공유하는 본문 마크업·스크립트를 `NoteTopics` 컴포넌트로 추출했다(`[area].astro` 11.4KB → 4.7KB).

## [1.3.0] - 2026-08-27

기출의 발문 형식에 맞춰 부정형 비중을 끌어올린 릴리스.

### 변경

- **부정형 발문 25% → 43%** — 긍정형 60문항을 부정형으로 재작성했다. 선지를 '참 3 + 거짓 1'로 다시 쓰고 정답 인덱스와 해설을 함께 갱신했으며, `id`·영역·난이도·주제는 보존해 저장한 문제와 오답노트의 참조가 끊기지 않게 했다. 거짓 선지는 두 개념의 정의를 맞바꾸거나 방향·순서를 뒤집는 방식으로 만들고, 나머지 참 선지 3개의 사실 여부를 전수 확인했다.
- **주제별 부정형 상한 50%** — 한 주제의 문항이 전부 부정형이 되지 않도록 배분했다.
- **발문 표현 분산** — 틀린 것 67 · 옳지 않은 36 · 가장 거리가 먼 14 · 해당하지 않는 13 · 아닌 것 13 · 적절하지 않은 4.

### 수정

- **정답 위치 편향 해소** — 부정형 문항의 정답이 4번에 46% 몰려 있어 내용을 몰라도 찍어서 맞힐 여지가 있었다. 선지 위치만 맞바꿔 재균형했다(38/34/36/41). 해설이 선지 번호를 언급하는 4문항과 숫자가 오름차순으로 놓인 선지는 제외했고, 정답으로 지목된 선지의 '내용'이 그대로인지 단언으로 검증했다.

## [1.2.0] - 2026-08-26

2022년 4월 필기 기출(해설집)을 대조해 개념 범위와 출제 형식을 맞춘 릴리스.
기출 문항은 복제하지 않고, 어떤 개념이 나오는지와 어떤 형식으로 묻는지만 참고했다.

### 추가

- **신규 12주제 + 기존 주제 보강 90문항** (은행 260 → 350, 주제 62 → 74) — 객체지향 기본 개념, 제품 소프트웨어 패키징과 DRM, 릴레이션의 구조와 무결성, 데이터베이스 설계 단계, 분산 데이터베이스, UNIX 운영체제, IP 프로토콜 헤더·전달 특성, 네트워크 토폴로지와 구성 장비, C 연산자와 제어문, Java 기본 문법, 악성코드 유형, 보안 위협 분류와 취약점 관리.
- **박스 제시문 도입** — `code` 필드를 코드 전용에서 제시문 블록으로 넓혀, 코드와 `[실행결과]`를 함께 주는 역방향 문항, `[조건]`과 `[SQL문]`의 빈칸 채우기, 표 데이터, 특징 불릿을 주고 용어를 고르게 하는 유형을 지원한다.
- **난이도 '상' 비중 13% → 19%** — 디스크 헤드 이동, 메모리 배치, 해시 충돌, IP 헤더 계산, 생산성 산정, 코드 추적 등 계산·추론형을 전수 검산해 추가했다.

### 변경

- **기존 17주제 도입부 보강** — 사용자/커널 수준 스레드, 모듈화, AAA와 인증 5유형, 명명된 공격 기법(Ping of Death·Smurf·Land·Teardrop·Piggyback), TCP Wrapper, HSM, IGP/EGP/AS, 생산성 산정식, 간트 차트 대 PERT, 유스케이스 관계 4종, UI 지침 확장·VUI·피드백, 버전 관리 도구, 통합 테스트의 깊이/넓이 우선과 스텁·드라이버 방향, xUnit 계열, IDE 기능, Python 자료형 분류, 구조체와 주소 계산.
- **부정형 발문 17% → 25%** — 기출은 약 50%가 부정형이고 표현도 "틀린 것은?"·"~이 아닌 것은?"·"가장 거리가 먼 것은?"으로 나뉘는데, 기존에는 "옳지 않은 것은?" 한 표현에 몰려 있었다. 표현을 분산하고 신규 문항의 49%를 부정형으로 작성했다.

## [1.1.0] - 2026-08-26

개념 노트를 "해설 모음"에서 "개념부터 읽는 노트"로 바꾼 릴리스.

### 변경

- **개념 주제를 블로그에서 분리** — 개념 노트의 묶음 단위가 `relatedPost`(블로그 슬러그)에서 `topic`으로 바뀌었다. `topic-notes.json`이 주제의 단일 정의(영역·제목·도입부·연결 글)를 갖고, 블로그 글은 주제의 선택 속성이 되어 글이 없는 주제도 도입부로 완결된다.
- **주제 재편 223문항 → 55주제** — 글이 없어 '개념 글 준비 중' 한 덩어리에 쌓여 있던 47문항(소프트웨어공학 31)이 사라지고 모든 주제가 도입부를 갖게 됐다.
- **오배치 문항 재배치** — 가장 가까운 글에 얹혀 있던 문항들을 제 주제로 옮겼다: 절차형 SQL·조회 문법을 SQL 분류에서, 테스트 레벨·도구를 블랙박스에서, 격리 수준을 트랜잭션에서, 스래싱·워킹셋을 페이지 교체에서, IPv6·NAT·DHCP를 서브넷팅에서, RIP/OSPF를 OSI에서, SQL 인젝션·시큐어 코딩을 접근통제에서, SYN 플러딩을 TCP에서 분리.
- **기존 도입부 보강** — 페이지 교체의 OPT·LFU·Clock, 관계대수의 조인 종류와 합병 가능 조건, 인덱스의 클러스터드 구분, GoF 23개 전체 목록, BLP의 읽기·쓰기 방향, 응집도 용어 표기 통일 등.

### 추가

- **신규 7주제 37문항** (은행 223 → 260, 주제 55 → 62) — 메모리 배치 전략과 단편화, 디스크 스케줄링과 파일 시스템, 해싱과 해시 테이블, 예외 처리와 언어 분류, 아키텍처 패턴, 코드 품질과 동료 검토, 신기술 동향. 난이도 '상'을 13문항 더해 비중이 10%에서 13%로 올랐다.
- **스키마 게이트** — 정의되지 않은 주제를 쓰거나 문항의 영역이 주제의 영역과 어긋나면 빌드가 실패한다.
- **공용 로더** `src/lib/bank.ts` — 주제의 글 링크를 문항에 얹어 모든 페이지가 같은 모양의 문항을 보게 한다.
- **푸터에 GitHub·버전 링크** — 버전은 `package.json`에서 빌드 시점에 주입된다.

### 수정

- 모바일에서 개념 노트 상단 칩이 가로 스크롤 대신 줄 바꿈되도록 변경. 주제 바로가기는 알약 칩과 구분되는 해시 링크 목차로 바꿔 영역 이동·난이도 필터와 혼동되지 않게 했다.
- 주간 블로그 싱크 리포트의 '도입부 갭'을 '글 연결 갭'(블로그 글감 후보)으로 전환.

## [1.0.0] - 2026-08-24

첫 공개 릴리스. 레포를 public으로 전환하고 GitHub Pages 자동 배포를 시작했다.
2026-08-15 스캐폴드부터 이 날까지의 개발 내용 전체를 포함한다.

### 문제 풀이

- 4지선다 연습 모드 — 영역·문항 수·난이도 선택 출제, 즉시 채점과 해설, 진행률 바·키보드 단축키
- 모의고사 모드 — 실제 시험 비중 배분 100문항·150분 타이머, OMR 팔레트, 과목별 과락(40점)/평균 60점 합격 판정
- 오답노트 — 오답 자동 누적, 오답만 다시 풀기, 정답 시 자동 제거
- 문제 은행 223문항 — 기출 비복제 자체 제작, zod 스키마가 발행 게이트(위반 시 빌드 실패), 전 문항 하/중/상 난이도 라벨
- 영역 6종 — 운영체제 · 네트워크 · 데이터베이스 · 소프트웨어공학 · 정보보안 · 프로그래밍(코드 해석 C/Java/Python·자료구조·알고리즘, 코드 블록 렌더링)

### 개념 노트

- 문항 해설을 영역·주제별로 모아 읽는 `/notes` 정적 페이지, 영역별 라우팅 분리
- 주제별 개념 도입부 — 30개 주제에 자족적 정의·핵심 정리(`topic-notes.json`), 해설 카드는 시험 포인트로 이어짐
- 확인 문제 퀴즈화(정답 가리기·선지 자기 점검), 난이도 필터, 읽기 진행률·읽던 위치 복원
- 역할별 내비게이션 구분 — 데스크톱 좌측 고정 목차, 모바일 영역 칩(줄 바꿈), 주제 바로가기는 해시 링크풍 목차
- 각 주제에서 개념을 다룬 블로그(daily.mcp) 글로 연결

### 계정·개인화 (Supabase)

- Google OAuth 로그인, 세션 만료 시 친절한 재로그인 안내
- 문제 저장(북마크)·개념 저장(☆), 오답 자동 기록 — RLS로 사용자별 격리
- 마이페이지 — 오답노트·저장한 문제·저장한 개념 보관함
- 무료 플랜 일시정지 방지 keep-alive 워크플로(3일 주기)

### UI/UX

- 다크모드 토글(localStorage 유지, 시스템 테마 기본), 한국어 어절 단위 줄바꿈
- 공용 헤더(모바일 햄버거 드로어)·푸터(AI 생성 콘텐츠 고지·기출 비복제·저작권)
- 페이지 셸 폭 1240px — 자매 블로그 daily.mcp와 통일

### 인프라·CI

- GitHub Pages 자동 배포 — main push 트리거(`deploy.yml`), Supabase 키는 Actions 시크릿으로 주입
- 블로그 동기화 도구(`scripts/blog-sync.mjs`) — prebuild 링크 검증·제목 자동화(`--check`), 죽은 링크·글 개정·커버리지 갭·도입부 갭 주간 리포트(`--report`)

[2.1.0]: https://github.com/mmyonaa/quiz/releases/tag/v2.1.0
[2.0.0]: https://github.com/mmyonaa/quiz/releases/tag/v2.0.0
[1.6.0]: https://github.com/mmyonaa/quiz/releases/tag/v1.6.0
[1.5.0]: https://github.com/mmyonaa/quiz/releases/tag/v1.5.0
[1.4.0]: https://github.com/mmyonaa/quiz/releases/tag/v1.4.0
[1.3.0]: https://github.com/mmyonaa/quiz/releases/tag/v1.3.0
[1.2.0]: https://github.com/mmyonaa/quiz/releases/tag/v1.2.0
[1.1.0]: https://github.com/mmyonaa/quiz/releases/tag/v1.1.0
[1.0.0]: https://github.com/mmyonaa/quiz/releases/tag/v1.0.0
